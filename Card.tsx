import React, { Component } from "react";
import { Text, View } from "react-native";

export class Card extends Component {
  render() {
    return (
      <View>
        <Text> textInComponent Card </Text>
      </View>
    );
  }
}

export default Card;
